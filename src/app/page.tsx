import AddImageForm from "@/components/AddImageForm";
import { Container } from "@mui/material";

const Home = async () => {
  return (
    <Container>
      <AddImageForm />
    </Container>
  );
};

export default Home;
